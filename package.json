{
  "name": "wardroai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "bash -c 'cd backend && source .venv/bin/activate && uvicorn main:app --reload'",
    "frontend": "bash -c 'cd frontend && npm run dev'",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
