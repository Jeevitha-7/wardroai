# Feature Specification: <Feature Name>

## Summary

Describe the user-visible capability and why it belongs in WardroAI.

## User Stories

- As a WardroAI user, I want <capability> so that <outcome>.

## Requirements

- The feature works offline on CPU.
- The feature does not call cloud image-analysis APIs.
- Streamlit remains the primary user interface unless the change explicitly targets legacy code.

## Acceptance Criteria

- [ ] `npm run lint` passes.
- [ ] `npm test` passes.
- [ ] `npm run coverage` passes the configured fail-under threshold.
- [ ] Documentation and examples are updated for user-visible analyzer changes.

## Out of Scope

List any explicit non-goals.
